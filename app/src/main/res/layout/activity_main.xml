<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity" >

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/c77_you_know_my_name_banner"
        tools:ignore="ContentDescription" />

    <EditText
        android:id="@+id/nama_edit_text"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:hint="@string/masukkan_namamu"
        android:textColor="@color/material_dynamic_secondary50"
        android:textSize="30sp"
        tools:ignore="Autofill,TextFields,VisualLintTextFieldSize" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/submit"
        android:layout_below="@id/nama_edit_text"
        android:textSize="30sp"
        android:onClick="submit_btn"
        tools:ignore="OnClick" />


</RelativeLayout>